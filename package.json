{
  "name": "denisa-bukovcanova-web",
  "version": "1.0.0",
  "private": true,
  "description": "Statický web — výživové poradenství (HTML/CSS/JS). Lokální skript na fotku: pip install -r scripts/requirements.txt && npm run process-photo",
  "scripts": {
    "dev": "serve . -l 5173",
    "start": "npm run dev",
    "preview": "npm run dev",
    "process-photo": "python scripts/process_hero_photo.py"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
