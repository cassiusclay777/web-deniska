{
  "name": "denisa-bukovcanova-web",
  "version": "1.0.0",
  "private": true,
  "description": "Statický web — výživové poradenství (HTML/CSS/JS). Lokální úprava fotky: pip install -r scripts/pillow.txt && npm run process-photo",
  "scripts": {
    "build": "node scripts/sync-public.js",
    "dev": "serve . -l 5173",
    "start": "npm run dev",
    "preview": "npm run dev",
    "process-photo": "python scripts/process_hero_photo.py"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
